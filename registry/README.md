# AGP Schema Registry 0.8

Validate the authoritative registry:

```bash
python registry/python/validate_registry.py registry/registry.json
```

Run the conformance suite:

```bash
python registry/tools/run_conformance.py
```
