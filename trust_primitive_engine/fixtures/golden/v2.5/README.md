# TPE 2.5 Contextual Predicates Golden Corpus

This corpus freezes deterministic end-to-end behavior for
`context_value_in`, `context_path_equals`, and `evidence_count_at_least`.

It covers one satisfied policy, one independent failure per predicate,
recursive failure projection, deterministic replay, compact serialization,
and SHA-256 result hashes.
