# TPE 2.0 conformance

Run from the repository root:

```bash
python trust_primitive_engine/tools/run_conformance.py
```

The Phase 1 suite contains 18 end-to-end cases covering primitive semantics,
identity normalization, validation, policy binding, signature rejection, and
byte-identical deterministic output.
